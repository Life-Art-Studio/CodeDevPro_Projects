DROP POLICY IF EXISTS "Enable all operations for customers" ON public.customers;
DROP POLICY IF EXISTS "Enable all operations for orders" ON public.orders;
DROP POLICY IF EXISTS "Enable all operations for products" ON public.products;
DROP POLICY IF EXISTS "Enable all operations for beats" ON public.beats;
DROP POLICY IF EXISTS "Enable all operations for visits" ON public.visits;
